[package]
name = "pfor"
version = "0.1.0"
edition = "2021"

# NOTE FROM TESTER:
# This file was written from the test branch and contains only [dev-dependencies].
# The implementer's Cargo.toml will contain [dependencies] and [[bin]] sections.
# Expect a merge conflict here at architect's merge of feat/pfor-core + test/pfor-core.
# The intended resolution is the UNION of both files' contents.

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
tempfile = "3"
